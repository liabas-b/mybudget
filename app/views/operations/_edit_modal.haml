#edit-operation-modal.modal.fade{:role => "dialog", :tabindex => "-1"}
  .modal-dialog.modal-lg{:role => "document"}
    .modal-content
      = form_for(@operation, remote: true) do |f|
        .modal-header
          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×
          %h4.modal-title= "OPERATION: #{f.object.name}"
        .modal-body
          = render 'operations/form_fields', f: f
        .modal-footer
          %button.btn.btn-success{:type => "submit"} SAUVEGARDER
          %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} FERMER
